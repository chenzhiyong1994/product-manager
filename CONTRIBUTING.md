# 贡献指南

感谢你愿意改进 ProductManager。本项目更看重可复现的问题和可验证的规则改进，而不是单纯增加模板篇幅。

## 适合贡献的内容

- 已脱敏的 PRD 失败案例或边界案例
- `create-prd` 的范围判断、章节规则和自检规则改进
- `check-prd` 的审查维度、风险项和可执行建议改进
- 能稳定复现行为差异的 `evals/` 用例
- README、示例和术语说明的纠错

## 隐私与内容要求

提交前请移除或替换：

- 客户、公司、学校、项目和个人的真实名称
- 邮箱、手机号、账号、用户 ID、工单号和业务数据
- 内网地址、带访问参数的分享链接、仓库地址和本机绝对路径
- API Key、Token、Cookie、浏览器会话和任何登录凭据
- 未获授权的访谈原文、需求单、截图、合同或内部文档

推荐使用 `示例公司`、`用户 A`、`PROJECT-001` 和 `https://example.com/...` 等清晰占位符。仅删除当前文件中的内容并不足以清除 Git 历史；如果误提交了凭据，请立即撤销或轮换凭据，并按 [`SECURITY.md`](SECURITY.md) 联系维护者。

## 修改原则

1. 先说明要解决的具体失败行为。
2. 修改离事实来源最近的文件：稳定流程放在 `SKILL.md`，细节放在对应 `references/`，回归场景放在 `evals/`。
3. 不要让小需求被迫使用完整系统模板，也不要通过降低标准遗漏研发和测试所需信息。
4. 新规则应说明适用条件；不适用的产品类型或文档范围应能被明确跳过。
5. 保持中文说明清晰、直接，代码符号、文件名和协议名保留英文。

## 本地检查

```powershell
git diff --check
Get-ChildItem skills -Recurse -Filter *.json | ForEach-Object { Get-Content -Raw $_ | ConvertFrom-Json | Out-Null }
```

此外请人工检查：

- Markdown 相对链接是否存在
- `SKILL.md` 的 YAML frontmatter 是否有效
- 新增示例是否完成脱敏
- 改动是否包含无关格式化或生成文件

## Pull Request

PR 描述请包含：

- 当前问题及一个最小复现输入
- 修改了哪些规则，为什么放在这些文件中
- 修改前后的预期行为差异
- 已运行的验证和仍未覆盖的风险

小而完整的改动更容易审查。若改动会显著改变工作流，建议先开 Issue 对齐适用场景和边界。
